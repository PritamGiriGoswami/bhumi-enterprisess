- [ ] Update CSS in styles.css to improve perceived image quality (reduce over-scaling/cropping artifacts; add better rendering defaults).
- [ ] Update index.html markup if needed (e.g., decoding/loading hints) while keeping existing PNGs.
- [ ] (Optional) Update stitch pages if they reuse same image quality issues (only if they are the ones user is viewing most).
- [ ] Run a quick local server and sanity-check in browser.

